// exports.sum = (a, b) => {
//   return a + b;
// };

// exports.mul = (a, b) => {
//   return a * b;
// };

//==es6===

const sum = (a, b) => {
  return a + b;
};

const mul = (a, b) => {
  return a * b;
};

export { sum, mul };
